{
  "name": "hemetaheme-subgraph",
  "version": "0.1.0",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "auth": "graph auth --product hosted-service",
    "deploy": "graph deploy --product hosted-service hemetaheme/hemetaheme",
    "create-local": "graph create --node http://localhost:8020/ hemetaheme/hemetaheme",
    "remove-local": "graph remove --node http://localhost:8020/ hemetaheme/hemetaheme",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 hemetaheme/hemetaheme"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.23.1",
    "@graphprotocol/graph-ts": "0.23.1"
  }
}
